---
name: poc-llama_index-CVE-2024-3098
description: 针对 CVE-2024-3098 的详细分析与 PoC 触发技能。当你具备受影响的llama_index版本时，使用此SKILL。
---
验证漏洞存在方法
    - 检查llama_index版本是否受CVE-2024-3098影响
    - 尝试通过safe_eval函数执行受限代码，观察是否能绕过限制
    - 验证是否可以通过提示注入执行任意系统命令

漏洞利用方法
    - 该漏洞存在于llama_index包的exec_utils类中的safe_eval函数
    - 由于输入验证不足，攻击者可以通过精心构造的提示注入绕过方法限制
    - 这是对先前已修复的CVE-2023-39662漏洞的绕过
    - 攻击者可以利用此漏洞在目标系统上执行任意代码
    
    - PoC示例：
      ```python
      # CVE-2024-3098 PoC for llama_index
      # 此PoC演示如何通过safe_eval函数绕过限制
      
      import llama_index
      
      # 尝试利用safe_eval函数的漏洞
      # 通过构造特定的输入来绕过限制
      malicious_input = "__import__('os').system('touch /tmp/pwned')"
      
      # 在受影响的版本中，此输入可能被成功执行
      # 导致在系统上创建/tmp/pwned文件
      
      # 注意：实际利用可能需要更复杂的payload构造
      # 以绕过safe_eval函数的安全检查
      ```
      
      **警告**: 此PoC仅用于教育和安全测试目的。在实际环境中使用前，请确保获得适当的授权。