ANTHROPIC_BASE_URL="xxxx"
ANTHROPIC_AUTH_TOKEN="xxxx" 
# ANTHROPIC_MODEL=your-model

# Stable CTF platform defaults. `SERVER_HOST` is the competition platform host,
# and `AGENT_TOKEN` is the team token used for platform auth.
SERVER_HOST=""
AGENT_TOKEN="xxxx"
# Optional MCP auth mode for the competition platform: `bearer` or `agent-token`
PLATFORM_MCP_AUTH_MODE="bearer"
